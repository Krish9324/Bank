Simple PNB project (student-level)
Run steps:
1) Import `backend/db.sql` into your MySQL (Workbench) to create database `pnb`.
2) In terminal: cd backend && npm install
3) Start backend: node server.js
4) In another terminal: cd frontend && npm install
5) Start frontend: npm start
Frontend assumes backend at http://localhost:4000
